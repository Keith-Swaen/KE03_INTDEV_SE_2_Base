@model DataAccessLayer.Models.Product
@{
    ViewData["Title"] = "Productinformatie";
}

<div class="container-fluid py-4">
    <div class="card shadow-sm" style="max-width:600px; margin:0 auto;">
        <div class="card-header bg-info text-white">
            <h3 class="card-title mb-0"><i class="fas fa-info-circle me-2"></i>Productinformatie</h3>
        </div>
        <div class="card-body">
            <dl class="row mb-4">
                <dt class="col-sm-4">Naam</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Name)</dd>
                <dt class="col-sm-4">Beschrijving</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.Description)</dd>
                <dt class="col-sm-4">Prijs</dt>
                <dd class="col-sm-8">€@Html.DisplayFor(model => model.Price)</dd>
                <dt class="col-sm-4">Voorraad</dt>
                <dd class="col-sm-8">@Html.DisplayFor(model => model.StockQuantity)</dd>
                <dt class="col-sm-4">Categorie</dt>
                <dd class="col-sm-8">@Model.Category?.Name</dd>
            </dl>
            <div class="d-flex gap-2">
                <a asp-action="Details" asp-controller="Orders" asp-route-id="@ViewBag.OrderId" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Terug naar bestelling
                </a>
            </div>
        </div>
    </div>
</div> 